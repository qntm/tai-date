{
  "name": "tai-date",
  "version": "1.0.7",
  "description": "A TaiDate stores an instant in TAI, the same way that a Date stores an instant in Unix time",
  "main": "./src/tai-date.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "t-a-i": "^1.0.5"
  },
  "repository": {
    "type": "git",
    "url" : "https://github.com/qntm/tai-date.git"
  }
}
